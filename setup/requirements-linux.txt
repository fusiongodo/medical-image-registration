# Linux + NVIDIA CUDA (RunPod etc.)
#
# bash remote/runpod_setup.sh
#
# Data pipeline on pod (subset of .data files — fresh linux_* annotations):
#   1. Put *.data in data/images/  (or: bash remote/runpod_download_images.sh)
#   2. bash remote/runpod_fetch_labels.sh
#   3. Run setup/quadtree_annotations.ipynb  -> data/linux_quadtree_annotations.json
#   4. Run setup/he_keypoint_annotations.ipynb -> data/linux_he_keypoint_annotations_superpoint.json
#   5. cd introducing_superpoint && python training.py
#
# CUDA PyTorch (if template lacks it):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

-r requirements.txt
